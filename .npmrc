; AXXA OS — npm config
; O `obsidian` (latest) declara peer @codemirror/state fixo (6.5.0) que conflita
; com as versões usadas pelo plugin. Esse conflito é pré-existente e benigno —
; legacy-peer-deps mantém o install reproduzível (inclusive no Storybook).
legacy-peer-deps=true
