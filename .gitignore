# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.swp

# Env / secrets
.env
.env.*
!.env.example

# Logs / temp
*.log
*.tmp

# Node
node_modules/
npm-debug.log
yarn-error.log

# Storybook (saída de build — gerada por `npm run build-storybook`)
storybook-static/

# Build output — TRACKED no git (necessário pra distribuição do plugin)
# Não ignorar: output/main.js, output/manifest.json, output/styles.css

# Config local (vault path do dev, etc.)
.axxa.local.json

# Config do vault Obsidian de teste (não faz parte do plugin)
.obsidian/

# Docs internos de orientação / planejamento (NÃO fazem parte do repo público
# nem da submissão ao Obsidian). Continuam no disco do dev, só saem do git.
# README.md é público e fica tracked.
ACTION-PLAN.md
AGENTS.md
QA-CHECKLIST.md
DESIGN-SYSTEM.md
MASTER-PLAN.md
PROMPT-MESTRE.md
VIABILIDADE-FINANCEIRA.md


# audit/reconcile scratch (não versionar)
.audit-*.cjs
.audit-*.mjs
.recon-*.json
.recon-*.cjs
